# 🗂 Allowlist Data Registry

Центральный реестр IPv4 диапазонов (CIDR) для системы мониторинга **Allowlist Scanner**.
Данные из этого репозитория автоматически синхронизируются с координационным сервером каждый час.

## 🛠 Инструменты

### [>> Генератор JSON (GUI) <<](https://IvanTrifonoff.github.io/allowlist-data/provider_generator.html)
Используйте этот веб-инструмент для быстрого форматирования списка CIDR в валидный JSON перед созданием Pull Request.

---

## 📥 Как добавить провайдера

Мы приветствуем помощь сообщества. Если вы знаете диапазоны IP провайдеров (VPS, Cloud, Hosting), которые стоит проверять, добавьте их сюда.

### Процесс (Workflow)

1.  **Fork:** Сделайте форк этого репозитория.
2.  **Generate:**
    *   Откройте [Генератор JSON](https://IvanTrifonoff.github.io/allowlist-data/provider_generator.html).
    *   Укажите название провайдера.
    *   Вставьте список CIDR (например, `10.20.30.0/24`).
    *   *Совет:* Поставьте `*` перед подсетью (например, `*192.168.1.0/24`), чтобы пометить её как **High Priority** (проверяется чаще).
    *   Нажмите "Сгенерировать" и скопируйте JSON.
3.  **Edit:**
    *   Откройте файл `providers.json` в вашем форке.
    *   Добавьте скопированные объекты в основной массив.
4.  **Pull Request:** Создайте PR в ветку `main`.
    *   GitHub Actions автоматически проверит валидность JSON и корректность CIDR.

## 📄 Формат данных (Spec)

Файл `providers.json` содержит массив объектов:

```json
[
  {
    "provider": "Selectel",       // Название группы/провайдера (String, Required)
    "cidr": "95.163.212.0/22",    // IPv4 диапазон в формате CIDR (String, Required)
    "priority": 10,               // 1 (Low), 5 (Normal), 10 (High) (Int, Default: 5)
    "comment": "Moscow Region"    // Комментарий (String, Optional)
  }
]
```

### Правила валидации
*   Корневой элемент должен быть списком (`[]`).
*   Поле `cidr` должно быть валидным IPv4 CIDR.
*   Дубликаты CIDR внутри базы игнорируются при синхронизации.

## 🔄 Синхронизация

*   **Периодичность:** Каждый час (Cron).
*   **Метод:** Сервер скачивает RAW версию `providers.json` из ветки `main`.
*   **Логика:** Новые CIDR добавляются в БД со статусом `PENDING`. Существующие записи обновляют свой `priority`.

---
License: MIT
