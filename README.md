# 🗂 Allowlist Data Registry

Центральный реестр IPv4 диапазонов (CIDR) для системы мониторинга **Allowlist Scanner**.
Данные из этого репозитория автоматически синхронизируются с сервером каждый час.

---

## ⚡️ Как добавить провайдера (Пошаговая инструкция)

Мы приветствуем помощь сообщества! Если вы знаете диапазоны IP российских хостингов или облаков, добавьте их сюда.

### Шаг 1. Подготовьте данные
Вам НЕ нужно писать код вручную. Используйте наш генератор:

👉 **[Открыть Генератор JSON](https://IvanTrifonoff.github.io/allowlist-data/provider_generator.html)**

1.  В поле **"Название Провайдера"** введите имя (например, `Selectel`).
2.  В поле **"Список подсетей"** вставьте список IP/CIDR (каждый с новой строки).
    *   *Совет:* Поставьте `*` перед важной подсетью (например, `*188.12.0.0/16`), чтобы сканировать её чаще (Приоритет 10).
3.  Нажмите **"Сгенерировать JSON"** и скопируйте полученный код в буфер обмена.

### Шаг 2. Добавьте данные в файл
1.  Откройте файл [**`providers.json`**](./providers.json) в этом репозитории.
2.  Нажмите иконку карандаша ✏️ (**Edit this file**) в правом верхнем углу.
    *   *Если у вас нет прав на запись, GitHub предложит создать Fork — нажмите зеленую кнопку "Fork this repository".*
3.  Прокрутите файл в самый низ (или в нужное место).
4.  Вставьте ваш скопированный JSON внутрь квадратных скобок `[...]`.
    *   ⚠️ **ВАЖНО:** Не забудьте поставить **запятую** `,` после предыдущего блока перед вашей вставкой!

    *Пример:*
    ```json
    [
      {
        "provider": "Old Provider",
        ...
      },  <-- ЗАПЯТАЯ ОБЯЗАТЕЛЬНА!
      {
        "provider": "Ваш Новый Провайдер",
        "cidr": "10.20.30.0/24",
        ...
      }
    ]
    ```

### Шаг 3. Сохраните и отправьте (Pull Request)
1.  Прокрутите страницу вниз до блока **"Commit changes"**.
2.  Напишите комментарий, например: `Add Selectel subnets`.
3.  Нажмите зеленую кнопку **"Propose changes"**.
4.  На следующей странице нажмите **"Create pull request"**.

**Готово!** 
Робот автоматически проверит формат данных. Если все хорошо (галочка ✅), администратор примет ваши изменения, и они попадут в базу данных в течение часа.

---

## 📄 Формат данных (Справочник)

Если вы хотите писать JSON вручную:

```json
[
  {
    "provider": "Selectel",       // Название (Обязательно)
    "cidr": "95.163.212.0/22",    // IPv4 CIDR (Обязательно)
    "priority": 10,               // 1=Low, 5=Normal, 10=High (По умолчанию: 5)
    "comment": "Moscow Region"    // Комментарий (Опционально)
  }
]
```

---
License: MIT